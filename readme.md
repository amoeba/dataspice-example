# dataspice-example

Example repo to show off [dataspice](https://github.com/ropenscilabs/dataspice).

```r
library(dataspice)
create_spice()

prep_biblio()
prep_coverage()
prep_attributes()
prep_attributes()

write_spice()
build_site()
```

